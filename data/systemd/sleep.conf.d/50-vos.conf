[Sleep]
# No swap in v1 → hibernate/hybrid disabled. This is a dpkg conffile:
# runtime overrides must land in a separate drop-in (60-vos-user.conf).
AllowSuspend=yes
AllowHibernation=no
AllowSuspendThenHibernate=no
AllowHybridSleep=no
