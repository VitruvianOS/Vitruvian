#!/bin/sh
# Create the standard Desktop shortcuts.
# Runs once per user (marker: ~/config/settings/first_login).
mkdir -p "$HOME/Desktop"
ln -sfn ..      "$HOME/Desktop/Home"
ln -sfn /system "$HOME/Desktop/System"
