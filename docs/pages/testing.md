# Testing {#testing}

## Running the test suite

The test suite only runs from within Vitruvian. It will fail if you try to run it from your cross-compile environment.

```bash
cd ~/code/Vitruvian # or wherever your self-hosted build is
./src/tests/runsuite.md
```
